spring.application.name=device-inventory

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Cambia entre perfiles con:
# - test (H2): -Dspring.profiles.active=test
# - dev (MySQL): -Dspring.profiles.active=dev

#mvn spring-boot:run -Dspring-boot.run.profiles=test (H2)
#mvn spring-boot:run -Dspring-boot.run.profiles=dev (MySQL)


spring.profiles.active=test
logging.level.org.springframework.core.env=INFO
